# Question 10 (Reference: helloinputstriptitle.py)
# In a single line, prompt the user "What's your name? " while immediately chaining .strip().title().
# On the next line, print "hello, {name}" using an f-string.
